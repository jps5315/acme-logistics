[build]
dockerfilePath = "Dockerfile"
buildArgs = { VITE_API_BASE_URL = "https://graceful-transformation-production-4213.up.railway.app" }

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
